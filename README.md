# Depom

This repository contains a sample WordPress theme named **Dating App Theme**. The theme files are located in the `dating-theme` directory.

## Usage

Copy the `dating-theme` folder to your WordPress `wp-content/themes` directory and activate it from the admin panel.
